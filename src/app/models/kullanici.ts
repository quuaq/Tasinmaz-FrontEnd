// src/app/models/kullanici.ts
export interface Kullanici {
  kullanici_id: number; // Backend'den dönen ID alanı
  kullanici_name: string; // Backend'den dönen kullanıcı adı alanı
  role: string; // Backend'den dönen rol alanı
}
